class Theater {

    // Properties
    id;// Assigned in database and automatically increments.
    name;
    assignedSeats;
    numSeats;
    screenType; // e.g., "IMAX", "Standard", "3D"
    seat;

    constructor() {
        
    }

}